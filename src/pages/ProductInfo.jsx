import React from 'react';
import Layout from '../components/Layout';

const ProductInfo = () => {
    return (
        <Layout>
            p info
        </Layout>
    );
};

export default ProductInfo;